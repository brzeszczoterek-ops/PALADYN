[profile.default]
src = "src"
test = "test"
out = "out"
cache_path = "cache"
optimizer = true
optimizer_runs = 200
evm_version = "prague"

[fuzz]
runs = 256

[invariant]
runs = 64
depth = 32
fail_on_revert = false
